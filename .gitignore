# Rust
/target
**/*.rs.bk

# Node
node_modules/

# Vite output, embedded into the release binary at build time.
# Checked in only as an empty directory so rust-embed has a folder to point at.
/dist-ui/*
!/dist-ui/.gitkeep
HANDOFF.md

# Editors / OS
.DS_Store
.idea/
*.swp
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
